# Security Rules

Rules:

1. Never hardcode passwords.
2. Never hardcode API keys.
3. Validate all external input.
4. Use parameterized SQL queries.
5. Avoid shell execution from user input.
6. Avoid disabling authentication checks.
7. Secrets must come from environment variables.